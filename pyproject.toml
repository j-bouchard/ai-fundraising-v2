[project]
name = "ai-fundraising-security"
version = "0.1.0"
description = "Security validation and audit tooling for Resin Cloudflare Workers"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.31.0",
]

[tool.uv]
managed = true
